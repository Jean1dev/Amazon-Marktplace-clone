server:
  port: 8082

spring:
  application:
    name: pedidos-query

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgresSQLDialect

  datasource:
    plataform: postgres
    url: jdbc:postgresql://localhost:5432/pedido_query
    username: jeanfernandes
    password: admin
    driveClassName: org.postgresql.Driver

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest