server:
  port: 8081

spring:
  application:
    name: produtos-core

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      dialect: org.hibernate.dialect.PostgresSQLDialect

  datasource:
    plataform: postgres
    url: jdbc:postgresql://localhost:5432/produto_core
    username: jeanfernandes
    password: admin
    driveClassName: org.postgresql.Driver

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest